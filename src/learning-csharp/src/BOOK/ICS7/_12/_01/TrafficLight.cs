namespace LT.BOOK.ICS7._12._01
{
    //----------------------------------------------------------------------------------------------------
    // enum 是 value type,
    // 所有的 member 具有相同的类型，这个类型默认是 int，
    // 所有的 member 的值默认从 0 开始，依次递增，
    //----------------------------------------------------------------------------------------------------
    public enum TrafficLight
    {
        Green,
        Yellow,
        Red
    }
}
